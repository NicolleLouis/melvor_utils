from models.mastery import Mastery


class AstrologyMastery:
    VARIEL = Mastery(
        current_mastery=2207451,
        action_time=2.4,
        mastery_per_action=194,
        input_per_action=1,
    )

    HAEMIR = Mastery(
        current_mastery=0,
        action_time=2.4,
        mastery_per_action=17,
        input_per_action=1,
    )
