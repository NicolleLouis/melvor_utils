from models.mastery import Mastery

class FletchingMastery:
    C_JAV = Mastery(
        current_mastery=5018000,
        action_time=1.50,
        mastery_per_action=626,
        input_per_action=3,
    )

    ONYX_BOLT = Mastery(
        current_mastery=1727708,
        action_time=1.50,
        mastery_per_action=551,
        input_per_action=1,
    )
