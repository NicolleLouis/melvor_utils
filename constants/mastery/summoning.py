from models.mastery import Mastery

class SummoningMastery:
    SALAMANDER = Mastery(
        current_mastery=712038,
        action_time=4.95,
        mastery_per_action=625,
        input_per_action=1,
    )

    CROW = Mastery(
        current_mastery=1798808,
        action_time=4.95,
        mastery_per_action=702,
        input_per_action=1,
    )
