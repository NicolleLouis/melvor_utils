from models.mastery import Mastery

class SmithingMastery:
    C_JAV_Head = Mastery(
        current_mastery=9860000,
        action_time=1.65,
        mastery_per_action=1755,
        input_per_action=2,
    )
